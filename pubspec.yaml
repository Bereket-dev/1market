name: koolan
description: "Koolan – Jigjiga's marketplace for buying, selling, hiring and finding services."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  cached_network_image: ^3.3.1
  flutter_cache_manager: ^3.4.1
  shared_preferences: ^2.5.3
  supabase_flutter: ^2.15.4
  flutter_dotenv: ^5.1.0
  google_sign_in: ^6.1.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.6
  permission_handler: ^12.0.0
  http: ^1.6.0
  google_generative_ai: ^0.4.6
  uuid: ^4.5.3
  file_picker: ^8.1.7
  connectivity_plus: ^7.3.0
  crypto: ^3.0.6
  path: ^1.9.1
  url_launcher: ^6.3.1
  firebase_core: ^3.13.1
  firebase_messaging: ^15.2.5
  flutter_local_notifications: ^18.0.1
  geolocator: ^13.0.2
  share_plus: ^10.1.4

dependency_overrides:
  # Flutter 3.32.0's bundled flutter_localizations uses intl.DateSymbols which
  # was removed in intl 0.20.0. Force 0.19.x until the SDK is upgraded.
  intl: '>=0.19.0 <0.20.0'

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Launcher icon generator — run: flutter pub run flutter_launcher_icons
  # Place brand artwork at assets/brand/logo.png (1024×1024) before running.
  flutter_launcher_icons: ^0.14.3

  # Native splash screen generator — run: flutter pub run flutter_native_splash:create
  # Regenerates android/app/src/main/res/drawable/launch_background.xml and styles.
  flutter_native_splash: ^2.4.5

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/brand/
    - assets/promo/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# ── Launcher icon config (flutter_launcher_icons) ─────────────────────────────
# Run: flutter pub get && flutter pub run flutter_launcher_icons
# Place brand artwork at assets/brand/logo.png (1024×1024, transparent or solid
# brand background) before running. For adaptive icons also provide:
#   assets/brand/logo_foreground.png  — safe-zone foreground layer
flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/brand/logo.png"
  # Adaptive icon (Android 8+)
  adaptive_icon_background: "#00288E"        # Koolan brand blue
  adaptive_icon_foreground: "assets/brand/logo_foreground.png"
  # Keep the notification icon as-is (monochrome drawable/ic_notification.xml)
  remove_alpha_ios: false
  min_sdk_android: 21

# ── Native splash config (flutter_native_splash) ──────────────────────────────
# Run: flutter pub get && flutter pub run flutter_native_splash:create
# To revert: flutter pub run flutter_native_splash:remove
flutter_native_splash:
  # Background colour matches the brand blue used on the launcher icon.
  color: "#00288E"
  # Centred logo shown on the splash — place artwork here before generating.
  # The image should be ~288×288 px (1x) for a comfortable safe zone.
  image: assets/brand/splash_logo.png
  # Dark-mode variant (same logo, darker bg)
  color_dark: "#001A5C"
  image_dark: assets/brand/splash_logo.png
  # Android 12+ (SplashScreen API)
  android_12:
    color: "#00288E"
    image: assets/brand/splash_logo.png
    color_dark: "#001A5C"
    image_dark: assets/brand/splash_logo.png
    icon_background_color: "#00288E"
    icon_background_color_dark: "#001A5C"
  # Keep splash visible until Flutter calls FlutterNativeSplash.remove()
  # — not used in this project; the splash hides when the first frame renders.
  fullscreen: false
